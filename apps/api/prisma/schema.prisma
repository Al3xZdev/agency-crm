// Slice 1 scaffold stub: generator + datasource wiring only.
// Task 1.3 replaces this file with the complete data model per design §1.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
